const Roles = ["ADMIN", "MANAGER", "MEMBER"];

const TaskPriorities = ["LOW", "MEDIUM", "HIGH"];

const TaskStatuses = ["TODO", "IN_PROGRESS", "IN_REVIEW", "DONE", "BLOCKED"];

module.exports = { Roles, TaskPriorities, TaskStatuses };

